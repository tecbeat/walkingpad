# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/

# Tools
.mypy_cache/
.ruff_cache/
.pytest_cache/

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store

# Local dev stack (HA container config)
dev/homeassistant/*
!dev/homeassistant/configuration.yaml
!dev/homeassistant/.gitkeep

# Local ESPHome dashboard state and secrets
dev/esphome/.esphome/
dev/esphome/secrets.yaml
dev/esphome/*.bin
dev/esphome/.device-builder*

# Playwright MCP snapshots (from browser-driven manual tests)
.playwright-mcp/

# CI ephemeral artifacts (produced by templater / version_bumper and
# consumed by the updater job — never committed to the repo).
updater.yml

.env
test-secrets.env
.tmp/
